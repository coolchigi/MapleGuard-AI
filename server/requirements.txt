# MapleGuard server runtime dependencies.
# The deterministic core (crs/pnp/paths/noc/ingest) needs NONE of these — it is pure stdlib
# Python and its tests run with nothing installed. These are for the API, the model calls, and
# the AgentCore deploy seams.

# HTTP API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
# ASGI->Lambda adapter for the API Lambda / Function URL deploy (api/lambda_handler.py).
mangum>=0.17

# Real Claude model for the NOC matcher/corrector (anthropic backend or Bedrock)
anthropic>=0.39

# boto3 is pulled in transitively by strands-agents / bedrock-agentcore, but we import it
# directly (BedrockModel, AnthropicBedrock, DynamoDB/SNS stores), so declare it explicitly.
boto3>=1.35

# typing_extensions is imported directly by agent/schemas.py: pydantic needs
# typing_extensions.TypedDict on Python < 3.12 (the AgentCore runtime is 3.11 today). Present
# transitively via pydantic, but declared here because we import it directly. Once the runtime is
# on 3.13 and schemas.py switches back to typing.TypedDict, this line can go.
typing_extensions>=4.0

# Agent layer + AgentCore seams (optional at runtime; imported lazily).
# Pinned exactly: Strands' default Bedrock model id changes across releases, and the deployed
# orchestrator now pins its own model id (agent.config.DEFAULT_BEDROCK_MODEL_ID) — pinning the
# SDK too keeps the hook/session/telemetry APIs we build against from shifting under us.
strands-agents==1.54.0
bedrock-agentcore==1.22.0
